-- 코드를 입력하세요
select name, count(name) 'count'
from ANIMAL_INS
group by NAME
having count(name)>1
order by name














# SELECT name, count(name) as 'count'
# from ANIMAL_INS
# # where name is not null
# group by name
# having count(name) > 1 
# order by name